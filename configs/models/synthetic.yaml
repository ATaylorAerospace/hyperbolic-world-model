# CPU fixture: frozen random projection encoder. For CI and tests only; never for reported results.
name: synthetic
embed_dim: 16
encoder:
  frozen: true
  frame_shape: [1, 8, 8]
  seed: 0
head:
  type: hyperbolic
  latent_dim: 8
  hidden_dim: 64
  n_layers: 2
  action_embed_dim: 16
  embed_scale: 1.0
  max_step: 5.0
  max_radius: 8.0
  seed: 0
