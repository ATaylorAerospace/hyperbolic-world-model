# Lorentz / hyperboloid model. Isometric to the Poincaré ball of the same curvature but
# numerically better behaved far from the origin. Curvature K < 0 is swept.
# `curvature` is passed to the constructor as `c` (see geometry/__init__.py::build_manifold).
name: lorentz
curvature: -1.0
